# Cloudflare Workers — static assets at site root; `index.html` is the live news feed UI.
# Worker name: letters, numbers, and hyphens only (no underscores).
name = "test-dev-file-for-cursor"
compatibility_date = "2024-11-12"

[assets]
directory = "static"
